// Learnova / Client / src / pages / students / Player.jsx
import React from "react";

const Player = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">Player</h1>
    </div>
  );
};

export default Player;
