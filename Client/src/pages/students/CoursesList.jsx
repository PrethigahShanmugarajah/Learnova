// Learnova / Client / src / pages / students / CoursesList.jsx
import React from "react";

const CoursesList = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">CoursesList</h1>
    </div>
  );
};

export default CoursesList;
