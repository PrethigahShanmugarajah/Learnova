// Learnova / Client / src / components / Loading.jsx
import React from "react";

const Loading = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">Loading</h1>
    </div>
  );
};

export default Loading;
