// Learnova / Client / src / components / students / TestimonialSection.jsx
import React from "react";

const TestimonialSection = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">TestimonialSection</h1>
    </div>
  );
};

export default TestimonialSection;
