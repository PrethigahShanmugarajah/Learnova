// Learnova / Client / src / components / students / Hero.jsx
import React from "react";

const Hero = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">Hero</h1>
    </div>
  );
};

export default Hero;
