// Learnova / Client / src / components / students / CoursesSection.jsx
import React from "react";

const CoursesSection = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">CoursesSection</h1>
    </div>
  );
};

export default CoursesSection;
