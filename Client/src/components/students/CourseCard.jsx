// Learnova / Client / src / components / students / CourseCard.jsx
import React from "react";

const CourseCard = () => {
  return (
    <div>
      <h1 className="text-green-500 bg-green-600">CourseCard</h1>
    </div>
  );
};

export default CourseCard;
